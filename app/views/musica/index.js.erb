var pagina = $('<%= escape_javascript(render(partial: "index")) %>')
$('.middle_wrapper').fadeOut('fast',function() {
	$('.middle_wrapper > div').remove()
	pagina.appendTo($(this))
	$(this).fadeIn('slow')		
})
